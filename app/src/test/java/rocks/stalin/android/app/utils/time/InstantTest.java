package rocks.stalin.android.app.utils.time;

import org.junit.Test;

import static org.junit.Assert.*;

public class InstantTest {/*
    @Test
    public void add() throws Exception {
        Clock.Instant i = Clock.fromNanos(123456789L);
        Clock.Duration d = new Clock.Duration(10000L, 10);

        Clock.Instant newInstant = i.add(d);

        //assertEquals(newInstant, Clock.fromNanos(10123456799L));
    }
*/
}